---
// TODO:
// - Implement optional hero image
// - Tags are filterable (and possibly a way to search posts)
// - Turn this into a re-usable component for
//   - Showing recent posts on home
//   - Creating a projects tab with the same functionality, plus some more things like an url and an image
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../../consts";
import { getCollection } from "astro:content";
import FormattedDate from "../../components/FormattedDate.astro";
import PostTags from "../../components/PostTags.astro";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <style lang="scss">
      main {
        width: 960px;
      }

      ul {
        list-style-type: none;
        padding: 0;

        li {
          &:not(:first-child) {
            margin-top: 0.5em;
          }

          a {
            text-decoration: none;

            &:hover h4,
            &:hover .date {
              color: var(--accent);
            }

            .date {
              margin: 0;
              color: var(--fg-dark);
            }

            .title {
              margin: 0;
              color: var(--fg);
              line-height: 1;
            }
          }
        }
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <section>
        <ul>
          {
            posts.map((post) => (
              <li class="post">
                <a href={`/blog/${post.slug}/`}>
                  <h4 class="title">{post.data.title}</h4>
                  <p class="date">
                    <FormattedDate date={post.data.pubDate} />
                  </p>
                  <PostTags {...post.data} />
                </a>
              </li>
            ))
          }
        </ul>
      </section>
    </main>
    <Footer />
  </body>
</html>
